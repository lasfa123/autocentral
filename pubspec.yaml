name: autocentral
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 🔹 Firebase
  firebase_core: ^2.28.0
  firebase_auth: ^4.6.2
  cloud_firestore: ^4.9.0
  firebase_storage: ^11.1.0
  intl: ^0.18.1
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.2

  # 🔹 File picker pour upload de documents
  file_selector: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
